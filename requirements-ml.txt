# Optional: Isolation Forest anomaly detection. Without these the detector
# falls back to a pure-Python z-score baseline and the API is unchanged.
-r requirements.txt
numpy==2.5.3
scikit-learn==1.9.0
